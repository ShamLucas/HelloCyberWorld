---
# Play 1 : Configuration de base (tous les hotes)
- name: Configuration de base
  hosts: all
  become: yes
  roles:
    - common

# Play 2 : Environnement dev (VM Vagrant)
- name: Dev (Vagrant)
  hosts: dev
  become: yes
  roles:
    - nginx

# Play 3 : Production (Raspberry Pi)
- name: Production (Raspberry Pi)
  hosts: raspberrypi
  become: yes
  roles:
    - pi-hardware
    - nginx
    - markdown-site
    - certbot
